# Example Secret Configuration for AWS EKS
# Create the actual secret using AWS Secrets Manager or kubectl:
#
# Option 1: Using kubectl
# kubectl create secret generic jhb-streampulse-ai-secret \
#   --from-literal=ANTHROPIC_API_KEY='your-api-key-here' \
#   -n jhb-streampulse
#
# Option 2: Using AWS Secrets Manager (recommended for production)
# 1. Store secret in AWS Secrets Manager
# 2. Use External Secrets Operator or AWS Secrets CSI Driver
# 3. Reference the secret in the deployment

apiVersion: v1
kind: Secret
metadata:
  name: jhb-streampulse-ai-secret
  namespace: jhb-streampulse
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "your-anthropic-api-key-here"
