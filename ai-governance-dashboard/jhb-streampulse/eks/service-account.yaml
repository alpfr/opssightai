apiVersion: v1
kind: ServiceAccount
metadata:
  name: jhb-streampulse
  namespace: jhb-streampulse
  labels:
    app: jhb-streampulse
  annotations:
    # If using IRSA (IAM Roles for Service Accounts), add the role ARN here:
    # eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/jhb-streampulse-role
